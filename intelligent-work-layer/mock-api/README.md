# Mock API payloads

This folder contains sample payloads that match the proposed Work OS UI contract.

## Files
- `work-os-view-model.json` — top-level payload for the shell and scenario surfaces

## Suggested usage
- Load the JSON payload in a mock API route for front-end development.
- Use the TypeScript fixtures under `src/mock-data/` for strongly typed local development.
